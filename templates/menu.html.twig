<nav class="navbar navbar-expand navbar-dark bg-dark">
	<a class="navbar-brand" href="{{path('home')}}">Accueil</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample02" aria-controls="navbarsExample02" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarsExample02">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item active">
				<a href="{{ path('security_register', {user_type:"user"})}}" class="btn btn-success">
					Inscription
				</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('admin')}}">Admin</a>
			</li>
			{% if app.user %}
				<li>
					<a href="{{ path('security_logout')}}" class="btn btn-danger ml-3">Déconnexion</a>
				</li>
			{% else %}
				<li>
					<a href="{{ path('security_register')}}" class="btn btn-success">Inscription</a>
				</li>
				<li>
					<a href="{{ path('security_login')}}" class="btn btn-info ml-3">Connexion</a>
				</li>
			{% endif %}
		</ul>
		<form class="form-inline my-2 my-md-0">
			<input class="form-control" type="text" placeholder="Search">
		</form>
	</div>
</nav>
